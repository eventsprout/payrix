<?php

declare(strict_types=1);

/*
 * Payrix
 *
 * This file was automatically generated by Worldpay.
 */

namespace PayrixLib\Controllers;

use Core\Request\Parameters\BodyParam;
use Core\Request\Parameters\HeaderParam;
use Core\Request\Parameters\QueryParam;
use Core\Request\Parameters\TemplateParam;
use Core\Response\Types\ErrorType;
use CoreInterfaces\Core\Request\RequestMethod;
use PayrixLib\Exceptions\ApiException;
use PayrixLib\Exceptions\ErrorFourHundredException;
use PayrixLib\Models\CustomersPostRequest;
use PayrixLib\Models\CustomersPutRequest;
use PayrixLib\Models\CustomersResponseResult;

class CustomersController extends BaseController
{
    /**
     * Query a Customer, which is a record that stores data about a particular customer of a Merchant and
     * can be used to associate payment data or with other API features.
     *
     * @param string $id The ID of this customer.
     * @param string|null $token A single-use, limited authentication layer in addition to an API
     *        Key or Session Key to authorize sensitive operations. <a href="/apis/payrix/dev-int-
     *        guide/references/authentication/auth-tokens" target="_blank">Auth Tokens</a> add
     *        security by requiring one-time approval for specific actions.
     * @param string|null $requestToken A custom, one-time identifier for any API request (GET, PUT,
     *        POST, or DELETE). Blocks future requests with the same token for 48 hours, ensuring
     *        only the first request is processed. Valid values can contain 1-100 alphanumeric and
     *        special characters. See <a href="/apis/payrix/dev-int-
     *        guide/references/authentication/request-tokens" target="_blank">Request Tokens</a>
     *        for more information.
     * @param string|null $embed Use the embed query parameter to include full object(s) of related
     *        resource(s) directly within the API response. This allows retrieval of associated
     *        resources in a single request, reducing the need for multiple round-trips.
     *        Format: GET https://{server}.payrix.com/{endpoint}?embed={relatedObject}
     *
     * @return CustomersResponseResult Response from the API call
     *
     * @throws ApiException Thrown if API call fails
     */
    public function getCustomersId(
        string $id,
        ?string $token = null,
        ?string $requestToken = null,
        ?string $embed = null
    ): CustomersResponseResult {
        $_reqBuilder = $this->requestBuilder(RequestMethod::GET, '/customers/{id}')
            ->auth('apiKey', 'sessionKey', 'txnSessionKey')
            ->parameters(
                TemplateParam::init('id', $id),
                HeaderParam::init('token', $token),
                HeaderParam::init('request-token', $requestToken),
                QueryParam::init('embed', $embed)
            );

        $_resHandler = $this->responseHandler()
            ->throwErrorOn('400', ErrorType::init('Error', ErrorFourHundredException::class))
            ->type(CustomersResponseResult::class);

        return $this->execute($_reqBuilder, $_resHandler);
    }

    /**
     * Update a Customer. A Customer is a record that is used to store data about a particular customer of
     * a Merchant, which you can use to associate payment data or for use with other API features.
     *
     * @param string $id The ID of this customer.
     * @param CustomersPutRequest $body Update Customer Request
     * @param string|null $token A single-use, limited authentication layer in addition to an API
     *        Key or Session Key to authorize sensitive operations. <a href="/apis/payrix/dev-int-
     *        guide/references/authentication/auth-tokens" target="_blank">Auth Tokens</a> add
     *        security by requiring one-time approval for specific actions.
     * @param string|null $requestToken A custom, one-time identifier for any API request (GET, PUT,
     *        POST, or DELETE). Blocks future requests with the same token for 48 hours, ensuring
     *        only the first request is processed. Valid values can contain 1-100 alphanumeric and
     *        special characters. See <a href="/apis/payrix/dev-int-
     *        guide/references/authentication/request-tokens" target="_blank">Request Tokens</a>
     *        for more information.
     *
     * @return CustomersResponseResult Response from the API call
     *
     * @throws ApiException Thrown if API call fails
     */
    public function putCustomersId(
        string $id,
        CustomersPutRequest $body,
        ?string $token = null,
        ?string $requestToken = null
    ): CustomersResponseResult {
        $_reqBuilder = $this->requestBuilder(RequestMethod::PUT, '/customers/{id}')
            ->auth('apiKey', 'sessionKey', 'txnSessionKey')
            ->parameters(
                TemplateParam::init('id', $id),
                HeaderParam::init('Content-Type', 'application/json'),
                BodyParam::init($body),
                HeaderParam::init('token', $token),
                HeaderParam::init('request-token', $requestToken)
            );

        $_resHandler = $this->responseHandler()
            ->throwErrorOn('400', ErrorType::init('Error', ErrorFourHundredException::class))
            ->type(CustomersResponseResult::class);

        return $this->execute($_reqBuilder, $_resHandler);
    }

    /**
     * Delete a Customer. A Customer is a record that is used to store data about a particular customer of
     * a Merchant. You can use it to associate payment data to the Customer, or for use with other API
     * features.
     *
     * @param string $id The ID of this customer.
     * @param string|null $token A single-use, limited authentication layer in addition to an API
     *        Key or Session Key to authorize sensitive operations. <a href="/apis/payrix/dev-int-
     *        guide/references/authentication/auth-tokens" target="_blank">Auth Tokens</a> add
     *        security by requiring one-time approval for specific actions.
     * @param string|null $requestToken A custom, one-time identifier for any API request (GET, PUT,
     *        POST, or DELETE). Blocks future requests with the same token for 48 hours, ensuring
     *        only the first request is processed. Valid values can contain 1-100 alphanumeric and
     *        special characters. See <a href="/apis/payrix/dev-int-
     *        guide/references/authentication/request-tokens" target="_blank">Request Tokens</a>
     *        for more information.
     *
     * @return CustomersResponseResult Response from the API call
     *
     * @throws ApiException Thrown if API call fails
     */
    public function deleteCustomersId(
        string $id,
        ?string $token = null,
        ?string $requestToken = null
    ): CustomersResponseResult {
        $_reqBuilder = $this->requestBuilder(RequestMethod::DELETE, '/customers/{id}')
            ->auth('apiKey', 'sessionKey', 'txnSessionKey')
            ->parameters(
                TemplateParam::init('id', $id),
                HeaderParam::init('token', $token),
                HeaderParam::init('request-token', $requestToken)
            );

        $_resHandler = $this->responseHandler()
            ->throwErrorOn('400', ErrorType::init('Error', ErrorFourHundredException::class))
            ->type(CustomersResponseResult::class);

        return $this->execute($_reqBuilder, $_resHandler);
    }

    /**
     * Query customers. Each Customer record stores data about a particular customer of a Merchant and can
     * be used to associate payment data to the Customer and for other API features.
     *
     * @param string|null $token A single-use, limited authentication layer in addition to an API
     *        Key or Session Key to authorize sensitive operations. <a href="/apis/payrix/dev-int-
     *        guide/references/authentication/auth-tokens" target="_blank">Auth Tokens</a> add
     *        security by requiring one-time approval for specific actions.
     * @param string|null $requestToken A custom, one-time identifier for any API request (GET, PUT,
     *        POST, or DELETE). Blocks future requests with the same token for 48 hours, ensuring
     *        only the first request is processed. Valid values can contain 1-100 alphanumeric and
     *        special characters. See <a href="/apis/payrix/dev-int-
     *        guide/references/authentication/request-tokens" target="_blank">Request Tokens</a>
     *        for more information.
     * @param string|null $search Set this header to filter or sort the list of resources that the
     *        method returns.
     *        See [Searches](page:welcome#searches) for detailed information and examples on how
     *        to use search header.
     * @param string|null $totals To configure a request to return a total for all instances of a
     *        field in a result set,  use the totals header in the format
     *        `totals={operator}[]={key}`.  This will calculate the desired total and return it in
     *        the `details > totals` object of the response.  For instance, if you want to sum the
     *        `total` field of all transactions,  you would use the `sum` operator. The response
     *        will include the result set,  along with the calculated total in the `details`
     *        section. See [Collection Operators](page:welcome#collection-operators) for detailed
     *        information and examples on how to use totals header.
     * @param int|null $pageNumber Set this path parameter to request a specific page of records.
     *        For example, set `?page[number]=2` to retrieve the second page of records for this
     *        request.
     * @param int|null $pageLimit Set this path parameter to request up to a specific amount of
     *        records. By default 30 records are retrieved per page. The maximum limit that can be
     *        set is 100.
     *        For example, set `?page[limit]=50` to retrieve up to 50 records for this request.
     * @param string|null $embed Use the embed query parameter to include full object(s) of related
     *        resource(s) directly within the API response. This allows retrieval of associated
     *        resources in a single request, reducing the need for multiple round-trips.
     *        Format: GET https://{server}.payrix.com/{endpoint}?embed={relatedObject}
     *
     * @return CustomersResponseResult Response from the API call
     *
     * @throws ApiException Thrown if API call fails
     */
    public function getCustomers(
        ?string $token = null,
        ?string $requestToken = null,
        ?string $search = null,
        ?string $totals = null,
        ?int $pageNumber = null,
        ?int $pageLimit = null,
        ?string $embed = null
    ): CustomersResponseResult {
        $_reqBuilder = $this->requestBuilder(RequestMethod::GET, '/customers')
            ->auth('apiKey', 'sessionKey', 'txnSessionKey')
            ->parameters(
                HeaderParam::init('token', $token),
                HeaderParam::init('request-token', $requestToken),
                HeaderParam::init('search', $search),
                HeaderParam::init('totals', $totals),
                QueryParam::init('page[number]', $pageNumber),
                QueryParam::init('page[limit]', $pageLimit),
                QueryParam::init('embed', $embed)
            );

        $_resHandler = $this->responseHandler()
            ->throwErrorOn('400', ErrorType::init('Error', ErrorFourHundredException::class))
            ->type(CustomersResponseResult::class);

        return $this->execute($_reqBuilder, $_resHandler);
    }

    /**
     * Create a Customer record to store data about a particular customer of a Merchant, and to associate
     * payment data to the Customer and for other API features.
     *
     * @param CustomersPostRequest $body Create Customer Request
     * @param string|null $token A single-use, limited authentication layer in addition to an API
     *        Key or Session Key to authorize sensitive operations. <a href="/apis/payrix/dev-int-
     *        guide/references/authentication/auth-tokens" target="_blank">Auth Tokens</a> add
     *        security by requiring one-time approval for specific actions.
     * @param string|null $requestToken A custom, one-time identifier for any API request (GET, PUT,
     *        POST, or DELETE). Blocks future requests with the same token for 48 hours, ensuring
     *        only the first request is processed. Valid values can contain 1-100 alphanumeric and
     *        special characters. See <a href="/apis/payrix/dev-int-
     *        guide/references/authentication/request-tokens" target="_blank">Request Tokens</a>
     *        for more information.
     *
     * @return CustomersResponseResult Response from the API call
     *
     * @throws ApiException Thrown if API call fails
     */
    public function postCustomers(
        CustomersPostRequest $body,
        ?string $token = null,
        ?string $requestToken = null
    ): CustomersResponseResult {
        $_reqBuilder = $this->requestBuilder(RequestMethod::POST, '/customers')
            ->auth('apiKey', 'sessionKey', 'txnSessionKey')
            ->parameters(
                HeaderParam::init('Content-Type', 'application/json'),
                BodyParam::init($body),
                HeaderParam::init('token', $token),
                HeaderParam::init('request-token', $requestToken)
            );

        $_resHandler = $this->responseHandler()
            ->throwErrorOn('400', ErrorType::init('Error', ErrorFourHundredException::class))
            ->type(CustomersResponseResult::class);

        return $this->execute($_reqBuilder, $_resHandler);
    }
}
