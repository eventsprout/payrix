<?php

declare(strict_types=1);

/*
 * Payrix
 *
 * This file was automatically generated by Worldpay.
 */

namespace PayrixLib\Models;

use Core\Utils\CoreHelper;
use Exception;
use stdClass;

class MinPasswordComplexityEnum
{
    /**
     * At least one complexity factor.
     */
    public const ONE = 1;

    /**
     * At least two complexity factors.
     */
    public const TWO = 2;

    /**
     * At least three complexity factors.
     */
    public const THREE = 3;

    /**
     * At least four complexity factors.
     */
    public const FOUR = 4;

    private const _ALL_VALUES = [self::ONE, self::TWO, self::THREE, self::FOUR];

    /**
     * Ensures that all the given values are present in this Enum.
     *
     * @param array|stdClass|null|int $value Value or a list/map of values to be checked
     *
     * @return array|null|int Input value(s), if all are a part of this Enum
     *
     * @throws Exception Throws exception if any given value is not in this Enum
     */
    public static function checkValue($value)
    {
        $value = json_decode(json_encode($value), true); // converts stdClass into array
        if (CoreHelper::checkValueOrValuesInList($value, self::_ALL_VALUES)) {
            return $value;
        }
        throw new Exception("$value is invalid for MinPasswordComplexityEnum.");
    }
}
