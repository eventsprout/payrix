<?php

declare(strict_types=1);

/*
 * Payrix
 *
 * This file was automatically generated by Worldpay.
 */

namespace PayrixLib\Models;

use Core\Utils\CoreHelper;
use Exception;
use stdClass;

class VasEfeOffersOfferStatusEnum
{
    /**
     * Indicates that the cash advance application is active and the sub-merchant is currently repaying the
     * cash advance.
     */
    public const ACTIVE = 'ACTIVE';

    /**
     * Indicates that the sub-merchant has applied for a cash advance and is awaiting approval from the
     * vendor.
     */
    public const APPLIED = 'APPLIED';

    /**
     * Indicates that the sub-merchant's cash advance application has been approved by the vendor and the
     * sub-merchant is expected to start repaying the cash advance.
     */
    public const APPROVED = 'APPROVED';

    /**
     * Indicates that the cash advance application has expired and the sub-merchant is no longer eligible
     * to receive the cash advance.
     */
    public const EXPIRED = 'EXPIRED';

    private const _ALL_VALUES = [self::ACTIVE, self::APPLIED, self::APPROVED, self::EXPIRED];

    /**
     * Ensures that all the given values are present in this Enum.
     *
     * @param array|stdClass|null|string $value Value or a list/map of values to be checked
     *
     * @return array|null|string Input value(s), if all are a part of this Enum
     *
     * @throws Exception Throws exception if any given value is not in this Enum
     */
    public static function checkValue($value)
    {
        $value = json_decode(json_encode($value), true); // converts stdClass into array
        if (CoreHelper::checkValueOrValuesInList($value, self::_ALL_VALUES)) {
            return $value;
        }
        throw new Exception("$value is invalid for VasEfeOffersOfferStatusEnum.");
    }
}
